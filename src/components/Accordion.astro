---
const { lang } = Astro.props;
import { Image } from "astro:assets";
import Accordion1 from "../assets/images/accordion/Accordion1.svg";
import Accordion2 from "../assets/images/accordion/Accordion2.svg";
import Accordion3 from "../assets/images/accordion/Accordion3.svg";
import Accordion4 from "../assets/images/accordion/Accordion4.svg";
import Accordion5 from "../assets/images/accordion/Accordion5.svg";
import Accordion6 from "../assets/images/accordion/Accordion6.svg";

const title = {
    lv: "Mēs piedāvājam:",
    ru: "Мы предоставляем:",
    en: "We offer:",
};

const accordionItems = [
    {
        id: "accordion-item-1",
        imgSrc: Accordion1,
        title_lv: "Loksnes",
        title_ru: "Листы",
        title_en: "Sheets",
        body_lv:
            "Tiek izgatavotas speciālajās iekārtās, sagriežot putu poliuretāna blokus. Porolona loksnes tiek izmantotas matraču, mēbeļu detaļu un citu izstrādājumu ražošanā.",
        body_ru:
            "Изготавливаются на специальных станках путем разрезки блоков пенополиуретана. Листы поролона используются для производства матрасов, мебельных деталей и других изделий.",
        body_en:
            "We produce sheets by using special machines for cutting blocks from polyurethane foam. Foam rubber sheets are used for the production of mattresses, furniture parts and other products.",
    },
    {
        id: "accordion-item-2",
        imgSrc: Accordion2,
        title_lv: "Smalki sagrieztas putas",
        title_ru: "Измельчённый поролон",
        title_en: "Crushed foam rubber",
        body_lv:
            "Porolona detaļas, sasmalcinātas speciālajā iekārtā, tiek izmantotas spilvenu, segu un iepakojumu utt. piepildīšanai. Sasmalcināšanai tiek izmantots vidēja mīkstuma vieglais porolons.",
        body_ru:
            "Детали из поролона, измельченного на специальном оборудовании, используются как наполнитель для подушек, одеял, для упаковки и т.д. Для измельчения используется лёгкий поролон средней мягкости.",
        body_en:
            "Parts made of foam rubber are crushed on special equipment and are later used as a filler for pillows, blankets, packaging, etc. For grinding, a light foam of medium softness is used.",
    },
    {
        id: "accordion-item-3",
        imgSrc: Accordion4,
        title_lv: "Detaļas, sagataves mēbelēm un pārējā produkcija",
        title_ru: "Детали, заготовки для мебели и прочая продукция",
        title_en: "Parts, blanks for the furniture and other products",
        body_lv:
            "Tiek izgatavotas ar speciālu porolona pārstrādes iekārtu palīdzību pēc iepriekš saskaņotajiem rasējumiem. Pēc pasūtītāju vēlmēm mēs sagatavojam pusfabrikātus, savienojam detaļas no porolona, pielīmējam sinteponu, tīklu, aplīmējam ar līmplēvi utt.",
        body_ru:
            "Изготавливаются с помощью специализированного оборудования для переработки поролона в соответствии с заранее согласованными чертежами. В зависимости от потребностей заказчиков мы подготавливаем полуфабрикаты, соединяем детали из поролона, приклеиваем синтепон, сетку, покрываем клейкой плёнкой и т.д.",
        body_en:
            "Are manufactured using equipment specialized for processing foam rubber in accordance with pre-agreed drawings. Depending on the needs of our customers, we prepare semi-finished products, connect foam rubber parts, glue sintepon (polyester wadding), mesh, cover details with adhesive film, etc.",
    },
    {
        id: "accordion-item-4",
        imgSrc: Accordion6,
        title_lv: "Laminētais porolons",
        title_ru: "Ламинированный",
        title_en: "Laminated",
        body_lv:
            "Tiek veidoti standartizmēra porolona bloki, kurus izmanto lokšņu, detaļu utt. ražošanai. Izgatavojam dažāda veida porolona blokus, kuriem piemīt dažādas īpašības.",
        body_ru:
            "Формируются блоки поролона стандартных размеров, которые используются для переработки в листы, детали и т.д. Мы изготавливаем блоки поролона различного вида и обладающие различными свойствами.",
        body_en:
            "Foam rubber blocks of standard sizes are formed, which are used for processing into sheets, parts, etc. We produce blocks of foam rubber of various types and with different properties.",
    },
];
---

<section id="accordion-section">
    <div class="container">
        <div class="heading">
            <h1>{title[lang]}</h1>
        </div>
        <div class="accordion" id="accordion">
            {
                accordionItems.map((item, index) => (
                    <div class="accordion-item" data-index={index}>
                        <button
                            class="accordion-button"
                            type="button"
                            aria-expanded="false"
                            data-accordion-button
                        >
                            <div class="accordion-button-content">
                                <Image
                                    src={item.imgSrc}
                                    alt=""
                                    width={55}
                                    height={55}
                                    class="accordion-icon"
                                />
                                <span class="accordion-title">
                                    {item["title_" + lang]}
                                </span>
                            </div>
                            <svg
                                class="accordion-arrow"
                                width="20"
                                height="20"
                                viewBox="0 0 20 20"
                                fill="none"
                                xmlns="http://www.w3.org/2000/svg"
                            >
                                <path
                                    d="M5 7.5L10 12.5L15 7.5"
                                    stroke="currentColor"
                                    stroke-width="2"
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                />
                            </svg>
                        </button>
                        <div class="accordion-content" data-accordion-content>
                            <div class="accordion-body">
                                {item["body_" + lang]}
                            </div>
                        </div>
                    </div>
                ))
            }
        </div>
    </div>
</section>

<style>
    /* Container */
    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 20px;
        width: 100%;
    }

    /* Section */
    #accordion-section {
        padding: 100px 0;
        background: #fff;
    }

    /* Heading */
    .heading {
        text-align: center;
        margin-bottom: 80px;
    }

    .heading h1 {
        font-size: 40px;
        color: var(--main-accent);
        font-family: "Ubuntu", sans-serif;
        line-height: 45px;
        font-weight: 500;
        margin: 0;
    }

    /* Accordion */
    .accordion {
        max-width: 1100px;
        margin: 0 auto;
    }

    /* Accordion Item */
    .accordion-item {
        border-bottom: 1px solid rgba(0, 0, 0, 0.125);
    }

    .accordion-item:last-child {
        border-bottom: none;
    }

    /* Accordion Button */
    .accordion-button {
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: space-between;
        background: #ffffff;
        border: none;
        padding: 20px 24px;
        cursor: pointer;
        transition: all 0.3s ease;
        font-family: "Ubuntu", sans-serif;
        text-align: left;
    }

    .accordion-button:hover {
        background: #f8f9fa;
    }

    .accordion-button:focus {
        outline: none;
    }

    .accordion-button[aria-expanded="true"] {
        background: #ffffff;
    }

    .accordion-button-content {
        display: flex;
        align-items: center;
        gap: 20px;
        flex: 1;
    }

    .accordion-icon {
        width: 55px;
        height: 55px;
        flex-shrink: 0;
    }

    .accordion-title {
        font-size: 26px;
        line-height: 1.4;
        color: #5c5c5c;
        font-weight: 400;
    }

    .accordion-arrow {
        flex-shrink: 0;
        color: #5c5c5c;
        transition: transform 0.3s ease;
    }

    .accordion-button[aria-expanded="true"] .accordion-arrow {
        transform: rotate(180deg);
    }

    /* Accordion Content */
    .accordion-content {
        overflow: hidden;
        transition: height 0.3s ease;
        height: 0;
    }

    .accordion-content.active {
        border-top: 1px solid rgba(0, 0, 0, 0.125);
    }

    .accordion-body {
        padding: 24px;
        font-family: "Open Sans", sans-serif;
        font-size: 18px;
        line-height: 36px;
        color: #6f6f6f;
    }

    /* Tablet: 768px - 991px */
    @media only screen and (max-width: 991px) {
        .container {
            padding: 0 20px;
        }

        .accordion-title {
            font-size: 22px;
        }

        .accordion-icon {
            width: 45px;
            height: 45px;
        }

        .accordion-body {
            font-size: 16px;
            line-height: 32px;
        }
    }

    /* Large Mobile: 480px - 770px */
    @media only screen and (max-width: 770px) {
        #accordion-section {
            padding: 50px 0;
        }

        .heading {
            margin-bottom: 40px;
        }

        .heading h1 {
            font-size: 30px;
            line-height: 36px;
        }

        .accordion-button {
            padding: 16px 20px;
        }

        .accordion-button-content {
            gap: 15px;
        }

        .accordion-title {
            font-size: 18px;
            line-height: 1.3;
        }

        .accordion-icon {
            width: 40px;
            height: 40px;
        }

        .accordion-arrow {
            width: 18px;
            height: 18px;
        }

        .accordion-body {
            padding: 20px;
            font-size: 14px;
            line-height: 28px;
        }
    }

    /* Small Mobile: 320px - 479px */
    @media only screen and (max-width: 479px) {
        #accordion-section {
            padding: 40px 0;
        }

        .container {
            padding: 0 15px;
        }

        .heading {
            margin-bottom: 30px;
        }

        .heading h1 {
            font-size: 25px;
            line-height: 32px;
        }

        .accordion-button {
            padding: 16px;
        }

        .accordion-button-content {
            gap: 12px;
        }

        .accordion-title {
            font-size: 16px;
            line-height: 1.3;
        }

        .accordion-icon {
            width: 35px;
            height: 35px;
        }

        .accordion-arrow {
            width: 16px;
            height: 16px;
        }

        .accordion-body {
            padding: 16px;
            font-size: 13px;
            line-height: 24px;
        }
    }
</style>

<script>
    document.addEventListener("DOMContentLoaded", () => {
        const accordionButtons = document.querySelectorAll(
            "[data-accordion-button]",
        );

        accordionButtons.forEach((button) => {
            button.addEventListener("click", () => {
                const accordionItem = button.closest(".accordion-item");
                const accordionContent = accordionItem?.querySelector(
                    "[data-accordion-content]",
                ) as HTMLElement;

                if (!accordionContent) return;

                const isExpanded =
                    button.getAttribute("aria-expanded") === "true";

                // Close all other accordion items
                accordionButtons.forEach((otherButton) => {
                    if (otherButton !== button) {
                        const otherItem =
                            otherButton.closest(".accordion-item");
                        const otherContent = otherItem?.querySelector(
                            "[data-accordion-content]",
                        ) as HTMLElement;

                        if (otherContent) {
                            otherButton.setAttribute("aria-expanded", "false");
                            otherContent.style.height = "0px";
                            otherContent.classList.remove("active");
                        }
                    }
                });

                // Toggle current accordion item
                if (isExpanded) {
                    button.setAttribute("aria-expanded", "false");
                    accordionContent.style.height = "0px";
                    accordionContent.classList.remove("active");
                } else {
                    button.setAttribute("aria-expanded", "true");
                    accordionContent.classList.add("active");
                    const height = accordionContent.scrollHeight;
                    accordionContent.style.height = `${height}px`;
                }
            });
        });

        // Recalculate heights on window resize
        let resizeTimer: number;
        window.addEventListener("resize", () => {
            clearTimeout(resizeTimer);
            resizeTimer = window.setTimeout(() => {
                accordionButtons.forEach((button) => {
                    const isExpanded =
                        button.getAttribute("aria-expanded") === "true";
                    if (isExpanded) {
                        const accordionItem = button.closest(".accordion-item");
                        const accordionContent = accordionItem?.querySelector(
                            "[data-accordion-content]",
                        ) as HTMLElement;

                        if (accordionContent) {
                            const height = accordionContent.scrollHeight;
                            accordionContent.style.height = `${height}px`;
                        }
                    }
                });
            }, 250);
        });
    });
</script>
